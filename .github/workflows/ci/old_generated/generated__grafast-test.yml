name: test grafast

on: [push]

jobs:
  runtest:
    name: Run test
    uses: ./.github/workflows/test-base.yml
    with:
      prepackPackages: "{pg-sql2,tamedevil,grafast}"
      package: "grafast"
      testcommand: "test"
      args: "--ci"
    