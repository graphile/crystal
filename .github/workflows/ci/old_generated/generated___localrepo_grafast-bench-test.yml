name: test @localrepo/grafast-bench

on: [push]

jobs:
  runtest:
    name: Run test
    uses: ./.github/workflows/test-base.yml
    with:
      prepackPackages: "{pg-sql2,tamedevil,@localrepo/grafast-bench}"
      package: "@localrepo/grafast-bench"
      testcommand: "test"
      args: "--ci"
    