name: test postgraphile

on: [push]

jobs:
  runtest:
    name: Run test
    uses: ./.github/workflows/test-base.yml
    with:
      prepackPackages: "{pg-sql2,tamedevil,postgraphile}"
      package: "postgraphile"
      testcommand: "test:polymorphic"
      args: "--ci"