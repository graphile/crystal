query ForeignKey {
  relationNodes {
    nodes {
      aThingId
      bThingId
      thingByAThingId {
        id
        rowId
        note
      }
      thingByBThingId {
        id
        rowId
        note
      }
    }
  }
}
