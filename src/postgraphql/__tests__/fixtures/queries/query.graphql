query {
  ...query
  query {
    ...query
    query {
      ...query
      query {
        ...query
      }
    }
  }
}

fragment query on Query {
  node(__id: "hello") {
    __typename
    __id
  }
}
