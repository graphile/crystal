query {
  typesById(id: 50) {
    bigint
    boolean
    enum
    domain
    domain2
    compoundType { ...compoundType }
    nestedCompoundType {
      a { ...compoundType }
      b { ...compoundType }
    }
  }
}

fragment compoundType on CompoundType {
  a
  b
  c
  d
}
