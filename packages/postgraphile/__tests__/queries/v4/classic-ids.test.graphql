## expect(errors).toBeFalsy();
#> schema: ["a", "b", "c"]
#> subscriptions: true
#> classicIds: true
{
  allPosts(condition: { rowId: 1 }) {
    nodes {
      id
      rowId
      headline
    }
  }
  allEdgeCases(condition: { rowId: 2 }) {
    nodes {
      rowId
    }
  }
}
