## expect(queries).toHaveLength(2);
## expect(data.allMessagesConnection.edges).toHaveLength(6);
## expect(data.first3.edges).toHaveLength(3);
{
  allMessagesConnection {
    edges {
      cursor
      node {
        body
        author {
          username
          gravatarUrl
        }
      }
    }
  }
  first3: allMessagesConnection(first: 3) {
    edges {
      cursor
      node {
        body
        author {
          username
          gravatarUrl
        }
      }
    }
  }
}
