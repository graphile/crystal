{
  badlyBehavedFunction {
    nodes {
      ...PersonFragment
    }
    edges {
      cursor
      node {
        ...PersonFragment
      }
    }
  }
}

fragment PersonFragment on Person {
  nodeId
  id
  firstName
}
