## expect(errors).toBeFalsy()
#> schema: ["polymorphic"]
#> simpleCollections: "both"

query {
  withDetails: allRelationalTopics(condition: { updatedAt: null }) {
    nodes {
      id
      type
      createdAt
    }
  }
  justId: allRelationalTopics(condition: { updatedAt: null }) {
    nodes {
      id
    }
  }
}
