# Tests what happens when an non-existent typename is returned from planType
{
  brokenItem {
    __typename
    id
    name
    ... on Equipment {
      currentDurability
      maxDurability
    }
    ... on Consumable {
      effect
    }
  }
}
