## expect(errors).toBeFalsy()
#// expect(queries).toHaveLength(1)
## expect(data.forums[0].messagesListSet).toHaveLength(1)
## expect(data.forums[0].messagesListSet[0].every(message => message.featured === false)).toBe(true)
## expect(data.forums[1].messagesListSet).toHaveLength(2)
## expect(data.forums[1].messagesListSet[0]).toHaveLength(2)
## expect(data.forums[1].messagesListSet[0].every(message => message.featured === false)).toBe(true)
## expect(data.forums[1].messagesListSet[1]).toHaveLength(1)
## expect(data.forums[1].messagesListSet[1].every(message => message.featured === true)).toBe(true)
{
  forums {
    messagesListSet {
      body
      featured
    }
  }
}
